import HomeContent from '@/components/HomeContent';  // Make sure you import HomeContent if it's a separate component

export default function Home() {
  return (
    <div>
      <HomeContent />  {/* Correctly used HomeContent component */}
    </div>
  );
}
